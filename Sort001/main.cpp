#include "sortdialog.h"
#include <QApplication>
#include "dialog1.h"

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    SortDialog w;
    Dialog1 d1;

    if (SortDialog.)

    w.show();

    return a.exec();
}
